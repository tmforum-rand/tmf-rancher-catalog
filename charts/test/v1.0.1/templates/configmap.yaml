apiVersion: v1
data:
  DBHOST: localhost
  DBNAME: {{database.name}}
  DBPASSWORD: {{database.password}}
  DBPORT: "27017"
  DBPROT: mongodb
  DBUSER: {{database.user}}
kind: ConfigMap
metadata:
  name: tmf-config-ttri
